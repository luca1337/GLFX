#pragma once

#include <glad/glad.h>

namespace glfx
{
	/*auto SetDepthTesting(const bool enable)
	{
		if (enable) {
			glEnable(GL_DEPTH_TEST);
			glDepthFunc(GL_LESS);
		}
		else {
			glDisable(GL_DEPTH_TEST);
		}
	}

	auto SetDepthStencil(const bool enable)
	{
		if (enable) {
			glEnable(GL_STENCIL_TEST);
			glStencilFunc(
				GL_ALWAYS,
				1,
				0xFF
			);
		}
		else
		{
			glDisable(GL_STENCIL_TEST);
		}
	}*/
}